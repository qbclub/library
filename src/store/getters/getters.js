export default {
    user(state) {
        return state.user
    },
    books(state) {
        return state.books;
    },
    bookflow(state) {
        return state.bookflow
    },
    backend(state) {
        return state.backend
    }

}